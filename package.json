{
  "name": "matrix-ci-sample",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test": "jest --ci",
    "build": "node scripts/build.js",
    "summarize": "node scripts/summarize.js"
  },
  "devDependencies": {
    "jest": "^29.0.0"
  }
}
